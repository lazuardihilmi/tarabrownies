rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for all collections for robust development in the sandbox environment
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
